export type AccountType = {
    id: number;
    name: string;
    path: string;
};

export type AccountMetadata = {
    name: string
    accountType: AccountType;
    
    // TODO: Finish this, later.
}